import React from "react";
import Form from "./LoginForm/LoginForm";

const Login = () => {
  return (
    <div className="login">
      <Form selectedRole="Login" />
    </div>
  );
};

export default Login;
